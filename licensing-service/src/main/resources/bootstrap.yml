spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
    inetutils:
      preferredNetworks:
        - 192.168
        - 10.0
#      discovery:
#        enabled: true
#        serviceId: configserver
